// 按需全局引入 vant组件
import Vue from 'vue'
import { Button, List, Cell, Tabbar, TabbarItem, Row, Col, Icon, CellGroup } from 'vant'

Vue.use(List)
  .use(Cell)
  .use(Button)
  .use(Tabbar)
  .use(TabbarItem)
  .use(Row)
  .use(Col)
  .use(Icon)
  .use(CellGroup)
